# Global Assistance – Static Website
Simple responsive landing page built with HTML + SCSS (no frameworks).

## Usage
Open `index.html` in any browser.

## SCSS
To compile styles:
```sh
npm install -g sass
sass --watch assets/scss/main.scss assets/css/main.css
